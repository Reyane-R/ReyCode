#!/bin/sh

REYCODE_LOG_DIR="${REYCODE_LOG_DIR:-$HOME/Library/Logs/ReyCode}"
mkdir -p "$REYCODE_LOG_DIR"
chmod 700 "$REYCODE_LOG_DIR"

export ERL_CRASH_DUMP="$REYCODE_LOG_DIR/erl_crash.dump"
export ERL_CRASH_DUMP_BYTES="10485760"
export RELEASE_DISTRIBUTION="none"
